<%= form_with(model: controle_acesso, local: true) do |form| %>
  <% if controle_acesso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(controle_acesso.errors.count, "error") %> prohibited this controle_acesso from being saved:</h2>

      <ul>
      <% controle_acesso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
